describe('base uri', function() {
  beforeEach(module('rdf-schema-aligment'));
  var rdfSchemaAligment;
  beforeEach(inject(Refine={'actionAreas':[]}));


  // beforeEach(inject(function(_rdfSchemaAligment_) {
    // rdfSchemaAligment = new _rdfSchemaAligment_.rdfSchemaAligmentDialog();
  // }));

  // it('is valid default value', function() {
    // expect(rdfSchemaAligmentDialog.RdfSchemaAlignment.baseUri).toBe(rdfSchemaAligmentDialog.RdfSchemaAlignment._defaultNamespace || window.location.protocol + "//" + window.location.host + "/");
  // });
});